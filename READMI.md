This apps is a restaurant sistem for clients, chefs and admins

The API endpoints correspond to all collections and have:
 - GetAll
 - Get  by some parameter
 - Update and update an specific parameter
 - Delete
 
